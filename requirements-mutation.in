# Mutation testing extends the cross-platform development toolchain. Keep this
# separate because mutmut requires fork support and does not run natively on
# Windows.
-r requirements-dev.in
mutmut==3.7.0
toml==0.10.2
