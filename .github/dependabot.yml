version: 2
updates:
  - package-ecosystem: "pip"
    # Keep root locks and mirrored scaffold documentation pins synchronized.
    directories:
      - "/"
      - "/skills/repo-scaffold/assets"
    schedule:
      interval: "weekly"
    versioning-strategy: "increase-if-necessary"
    groups:
      synchronized-python:
        group-by: "dependency-name"
      synchronized-python-security:
        applies-to: "security-updates"
        patterns:
          - "markdown-it-py"
          - "PyYAML"
    commit-message:
      prefix: "chore(deps)"

  - package-ecosystem: "github-actions"
    # GitHub Actions Dependabot scans only .github/workflows at repository root.
    # template assets are synchronized by action-pin-sync.yml.
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      synchronized-actions:
        # One PR keeps sub-actions such as CodeQL init/analyze/upload-sarif on
        # the same release SHA across installed workflows and templates.
        applies-to: "version-updates"
        patterns:
          - "*"
      synchronized-actions-security:
        applies-to: "security-updates"
        patterns:
          - "*"
    commit-message:
      prefix: "chore(deps)"
